---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kro
  namespace: kro
spec:
  interval: 1m
  releaseName: kro
  chart:
    spec:
      chart: kro
      version: 0.2.0
      sourceRef:
        kind: HelmRepository
        name: kro
      interval: 1m
  install:
    remediation:
      retries: 5

  values:
