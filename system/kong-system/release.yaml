---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kong-gateway-operator
spec:
  interval: 5m
  chart:
    spec:
      chart: gateway-operator
      version: 2.41.1
      sourceRef:
        kind: HelmRepository
        name: kong
      interval: 1m
  values:
    image:
      tag: 1.3
