---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: clickhouse-operator
  namespace: db-operator-system
spec:
  interval: 5m
  chart:
    spec:
      chart: signoz
      version:
      sourceRef:
        kind: HelmRepository
        name: clickhouse-operator
      interval: 1m
  values:
