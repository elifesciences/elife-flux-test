---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: nidhogg
  namespace: nidhogg
spec:
  interval: 5m
  chart:
    spec:
      chart: nidhogg
      version: "0.1.0"
      interval: 5m
      sourceRef:
        kind: HelmRepository
        name: nidhogg
  values:
    image:
      tag: v0.5.1
    configuration:
      taintRemovalDelayInSeconds: 5
      nodeSelector:
      - karpenter.sh/nodepool
      daemonsets:
      - name: "spegel"
        namespace: "spegel"
