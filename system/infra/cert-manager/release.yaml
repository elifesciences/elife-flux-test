---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: cert-manager
  namespace: infra

spec:
  chart:
    repository: https://charts.jetstack.io
    name: cert-manager
    version: v1.7.1

  values:
    installCRDs: false
    prometheus:
      servicemonitor:
        enabled: true
