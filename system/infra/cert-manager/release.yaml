---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: infra-cert-manager
  namespace: infra

spec:
  interval: 5m
  chart:
    spec:
      chart: cert-manager
      version: v1.7.2
      sourceRef:
        kind: HelmRepository
        name: cert-manager
      interval: 1m

  values:
    installCRDs: false
    prometheus:
      servicemonitor:
        enabled: true
