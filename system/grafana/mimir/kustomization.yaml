apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- release.yaml
- datasource.yaml

configMapGenerator:
- name: kubernetes-mixin
  files:
  - kubernetes-mixin/prometheus_rules.yaml
  - kubernetes-mixin/prometheus_alerts.yaml
generatorOptions:
  disableNameSuffixHash: true
