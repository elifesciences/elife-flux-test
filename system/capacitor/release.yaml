---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: capacitor
spec:
  chart:
    spec:
      chart: capacitor-next
      sourceRef:
        kind: OCIRepository
        name: capacitor
  interval: 10m
  timeout: 5m
  values: {}
