---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: odigos
  namespace: signoz
spec:
  interval: 5m
  chart:
    spec:
      chart: odigos
      version: 0.3.5
      sourceRef:
        kind: HelmRepository
        name: odigos
      interval: 1m
  values:
