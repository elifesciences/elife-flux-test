---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: cert-manager
  namespace: adm

spec:
  chart:
    repository: https://charts.jetstack.io
    name: cert-manager
    version: 1.0.0

  values:
    installCRDs: true
    prometheus:
      servicemonitor:
        enabled: true
