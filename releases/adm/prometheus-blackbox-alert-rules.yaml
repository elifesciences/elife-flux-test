apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: blackbox.rules
  namespace: adm
  labels:
    app: blackbox
spec:
  groups:
  - name: ./blackbox.rules
    rules:
    - alert: BlackboxProbeFailed
      expr: probe_success != 1
      for: 5m