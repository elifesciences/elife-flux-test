---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: basex-validator
  namespace: stg
  annotations:
    fluxcd.io/automated: 'true'
    filter.fluxcd.io/basexValidator: glob:master-*-*

spec:
  releaseName: basex-validator--stg
  chart:
    git: https://github.com/elifesciences/basex-validator-formula
    path: helm/basex-validator
    ref: wip/migrate-to-new-cluster

  values:
    ingress:
      host: basex-validator--staging--test-cluster.elifesciences.org

    basexValidator:
      replicaCount: 1
      image:
        repository: elifesciences/basex-validator
        tag: master-21c13962-20200825.1546
