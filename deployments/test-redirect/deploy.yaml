apiVersion: apps/v1
kind: Deployment
metadata:
  name: echo
  namespace: test-redirect
  labels:
    app: echo
spec:
  template:
    spec:
      containers:
      - name: echo
        image: hashicorp/http-echo
        args:
        - "-text=hello"
        - "-listen=:8080"
