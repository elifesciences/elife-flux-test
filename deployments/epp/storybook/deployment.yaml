apiVersion: apps/v1
kind: Deployment
metadata:
  name: epp-storybook
  namespace: epp
  labels:
    app: epp-storybook
spec:
  replicas: 1
  selector:
    matchLabels:
      app: epp-storybook
  template:
    metadata:
      namespace: epp
      labels:
        app: epp-storybook
    spec:
      containers:
      - name: epp-storybook
        image: ghcr.io/elifesciences/enhanced-preprints-storybook:master-0c12273a-20221019.1331 # {"$imagepolicy": "epp:epp-storybook"}
        ports:
        - containerPort: 6006
