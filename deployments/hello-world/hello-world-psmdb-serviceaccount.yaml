apiVersion: v1
kind: ServiceAccount
metadata:
  name: hello-world-psmdb-serviceaccount
  namespace: hello-world
  annotations:
    eks.amazonaws.com/role-arn: "${aws_backup_service_account_role_arn}"
