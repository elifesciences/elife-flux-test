---
kind: Gateway
apiVersion: gateway.networking.k8s.io/v1
metadata:
  name: api-gateway
  namespace: gateway--test
spec:
  gatewayClassName: api-gateway
  listeners:
  - name: http
    protocol: HTTP
    port: 80
