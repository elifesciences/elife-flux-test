---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: mongodb
  namespace: epp

spec:
  interval: 1m
  releaseName: mongodb
  chart:
    spec:
      chart: psmdb-db
      version: 1.12.3
      sourceRef:
        kind: HelmRepository
        name: percona
        namespace: db-operator-system
      interval: 1m
  install:
    remediation:
      retries: 5

  values:
