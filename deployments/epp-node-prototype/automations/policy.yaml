apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
spec:
  imageRepositorySpec:
    name: epp-node-prototype
    namespace: epp
  filterTags:
    pattern: '^master-[a-fA-F0-9]+-(?P<ts>.*)'
    extract: '$ts'
  policy:
     numerical:
      order: asc
