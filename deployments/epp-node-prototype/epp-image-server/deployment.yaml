apiVersion: apps/v1
kind: Deployment
metadata:
  name: epp-image-server
  namespace: epp
  labels:
    app: epp-image-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: epp-image-server
  template:
    metadata:
      namespace: epp
      labels:
        app: epp-image-server
    spec:
      containers:
      - name: epp-image-server
        image: ghcr.io/elifesciences/epp-image-server:master-70be99d4-20220815.1804 # {"$imagepolicy": "epp:epp-image-server"}
        ports:
        - containerPort: 8182
